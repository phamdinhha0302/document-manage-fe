import type { ThemeConfig } from 'ant-design-vue/es/config-provider/context'

export const theme: ThemeConfig = {
    token: {
        fontFamily: `"Montserrat", "Roboto", sans-serif`,
        colorPrimary: '#48cae4' // Synced with tailwind.config.js
    }
}
